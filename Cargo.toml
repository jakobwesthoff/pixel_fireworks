[package]
name = "fireworks"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "*"
pixel_loop = { version = "*", path = "../pixel_loop", default-features = false, features = [
  "crossterm",
] }
